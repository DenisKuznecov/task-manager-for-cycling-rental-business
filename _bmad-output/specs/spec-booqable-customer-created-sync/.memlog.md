---
topic: Booqable customer.created/updated → Google Contacts, Holded, Mailchimp
goal: Replace the 300 EUR/month Zapier customer-sync with inspectable landings in the admin app
updated: 2026-08-31T15:12
---

- (note) Source: _bmad-output/brainstorming/brainstorm-booqable-customer-created-sync-2026-08-28/.memlog.md (brainstorm complete, MoSCoW locked).
- (decision) CAP-1: on Booqable customer.created and customer.updated, land the same person in Google Contacts, Holded, and Mailchimp; update must not create a second contact.
- (decision) CAP-2: thin /customers table behind the existing Customers nav shows one row per customer with three destination statuses and real errors; no retry button.
- (decision) Passport fields sent whole, never silently truncated: email, phone, name, address (country, region, city, street, zip), birthday.
- (decision) Green means the person is known to exist in that destination, not that every passport field landed perfectly.
- (decision) Mailchimp accepting the person but dropping address is not a must-stay-red.
- (decision) Retry is save-again in Booqable (customer.updated), not an in-app retry button.
- (decision) No owned identity store; day-one product is visible replicas in the tools they already use.
- (decision) Won't this time: full CRM, separate Sync page, retry button, stuck-red UI, address-drop-as-failure, owned identity store, three lights on a future customer card.
- (decision) Should = readable errors clearer than Zapier. Could = Mailchimp dual-fix copy (add region in Booqable or relax ADDRESS). Dual-fix sentence is not v1-required.
- (constraint) Destination credentials stay server-only environment variables, never in the browser, matching Booqable/Resend/Short.io.
- (constraint) Google Contacts writes cannot use a Google API key; they need OAuth 2.0 (user refresh token or Workspace domain-wide delegation).
- (question) Which Google account or Workspace directory receives contacts, and is it OAuth-once-plus-refresh-token or domain-wide delegation?
- (question) Which Mailchimp audience (list ID) and which account user's API key?
- (question) Which Holded company and API token scopes (contacts write)?
- (question) Do local/staging/preview disable outbound writes (like workshop sync) or use separate sandbox destination accounts?
- (question) Is historical backfill of existing Booqable customers in scope, or only new created/updated events after cutover?
- (question) Should company/organization Booqable records be skipped, or does 'human' mean every customer the current Zap already syncs?
- (assumption) Status lights attach to a local row keyed by booqable_customer_id; that row is visibility, not a CRM source of truth. Existing public.customers already has that unique key from workshop/order apply.
- (assumption) The existing Customers nav (admin/manager, no href) is the home for this table.
- (note) project-context.md was listed as a persistent fact but is absent from the repo; brownfield taken from architecture spine, existing webhook, and customers table.
- (event) Self-validate pass 1 (coherence): CAP-1 and CAP-2 have intent+success; intents are WHAT; constraints rule out fake-green, retry UI, CRM, API-key Google, shared prod credentials; non-goals present; success signal is demonstrable; IDs stable; prose lean. Pass.
- (event) Self-validate pass 2 (preservation): MoSCoW Must/Should/Could/Won't, passport fields, created+updated, person-exists green, address-drop ok, /customers nav, no retry button, 300 EUR Zap pain all landed in SPEC or companions. Wrapper-only dropped: technique names, partner-mode ceremony, discarded metaphors. Pass.
- (decision by user) HOLDED_API_KEY is in .env.local; Holded writes use that server-only key.
- (decision by user) MAILCHIMP_API_KEY is in .env.local. Audience ID 74fcbaad78 is stored as MAILCHIMP_AUDIENCE_ID in env, not in code.
- (decision by user) Customers nav confirmed as the home for the status table.
- (decision by user) The /customers row shows the customer name for identification; identity key stays booqable_customer_id.
- (decision by user) Local Booqable webhook to localhost may write the live Holded/Mailchimp/Google accounts. Preview/PR still must not write those live destinations.
- (decision by user) No historical backfill; only customer.created and customer.updated after cutover.
- (decision) Google Contacts auth path: one-time OAuth by the business Google account that owns the contacts, then store a refresh token as a server-only secret. Domain-wide delegation is not required for v1.
- (constraint) Do not subscribe customer.created/updated to the existing production order webhook until the handler can distinguish customer events from order events.
- (constraint) Booqable cannot reach http://localhost; a local subscription needs a public HTTPS tunnel (ngrok or Cloudflare Tunnel) to the same /api/webhooks/booqable?secret= path.
- (question) Which Google account email owns the shared contacts the Zap writes to today?
- (question) Should Booqable company/organization records be skipped, or does every customer the current Zap syncs land?
- (event) Self-validate pass 1 (coherence, update): CAP-1/CAP-2 still have intent+success; name-on-row is display not a second key; env isolation, no-backfill, no customer-on-order-webhook, tunnel-for-localhost all bend design; non-goals include backfill; two open questions remain. Pass.
- (event) Self-validate pass 2 (preservation, update): User answers landed — Holded/Mailchimp keys in env, audience 74fcbaad78 as MAILCHIMP_AUDIENCE_ID, name on /customers, nav yes, local live writes ok, preview still off, only new events, Google = OAuth refresh token. Wrapper-only: none dropped this pass. Pass.
- (note) Google Contacts product is written via People API. The old Contacts API (m8/feeds) was turned down 2022-01-19; do not enable Contacts API in Cloud Console.
- (note) Google Auth Platform Data Access UI: add only https://www.googleapis.com/auth/contacts via Add or remove scopes; it appears under Sensitive scopes.
- (note) OAuth Playground 403 access_denied while signed in as denys.kuznetsov@echeloncyclinghub.com: app is Testing and that user must be a listed test user, or Audience must be Internal Workspace.
- (decision) Google OAuth Audience is External + Testing. Internal is unavailable: Cloud project is not under Google Workspace. Add test users; do not publish.
- (decision by user) Google Contacts destination and OAuth test user is echeloncyclinghub@gmail.com. admin@echeloncyclinghub.com is not a Google Account.
- (event) User reports Google client id/secret/refresh token (and destination keys) are in local env. Google setup leftover closed. Do not subscribe customer webhooks until handler can distinguish events.
- (event) Handover written to HANDOVER.md for a new session. Spec not listed as companion (process, not contract). Next: company/org question, story breakdown, or implement after webhook can distinguish events.
- (decision by user) Land every Booqable customer the current Zap lands, including company/organization records; do not skip them.
- (note) Existing local ngrok and production already subscribe to order webhook events at /api/webhooks/booqable. Those subscriptions stay. Customer.created/updated need new Booqable subscriptions.
- (note) Current receiver reads only form data[id] and always calls reconcileBooqableOrder. A customer id on that path today is treated as an order id.
- (question) Should customer.created/updated use a dedicated listener URL, or the same /api/webhooks/booqable path after it can distinguish event types?
- (assumption) Existing webhook body is form-encoded data[id] only; tests do not show an event-type field. Same-URL routing is unsafe until a delivery names the event.
- (event) Self-validate pass 1 (coherence, update): CAP-1 success now includes company/org; new constraints cover leave-order-subs-alone and never-hit-order-only-handler; URL shape is open; intents WHAT; IDs stable. Pass.
- (event) Self-validate pass 2 (preservation, update): Land-everyone, existing ngrok+prod order subs, data[id]-as-order, new customer subscriptions, same-vs-dedicated URL question all landed. Wrapper-only: none. Pass.
- (decision by user) One URL: /api/webhooks/booqable for order and customer events. Fail-closed parse of top-level event first. Add customer.created and customer.updated subscriptions only after that parse is live in that environment. Do not recreate existing order subscriptions.
- (decision) Captured delivery body has top-level event (e.g. order.reserved), object, id, version, and data[*]. Handler must read event; missing/unknown event is 200 and no write, never default to order reconcile.
- (event) Self-validate pass 1 (coherence, update): URL question closed; fail-closed + subscribe-after-live are constraints; webhook-cutover.md holds the sequence; no open questions. Pass.
- (event) Self-validate pass 2 (preservation, update): One-URL, captured event body, add-not-recreate, local-then-prod subscribe, ngrok-host-change all landed in SPEC or webhook-cutover.md. Wrapper-only: none. Pass.
- (decision) Booqable subscriptions are API webhook_endpoints (url + events array), not a Settings UI. After fail-closed, PATCH the existing local then production endpoint to add customer.created and customer.updated; keep order events.
- (event) Self-validate: cutover companion now matches the historical curl (POST/PATCH /api/4/webhook_endpoints). No secrets written. Pass.
- (note) Listed webhook_endpoints 2026-08-31: local 052183d7… ngrok order.*; prod 03a500e7… vercel order.*; Zapier f518c090… customer.created only; leave other Zapier/Holded rows.
- (decision by user) Ngrok host confirmed as reprocess-construct-backache.ngrok-free.dev.
- (event) Self-validate pass 1 (finalize): CAP-1/CAP-2 intent+success; fail-closed, PATCH-after-live, Zapier-leave-until-proven, company/org-land, one URL all bend design; no open questions; IDs stable. Pass.
- (event) Self-validate pass 2 (finalize): Endpoint ids, Zapier customer.created, captured event body, ngrok host, land-everyone, OAuth mailbox all in SPEC or companions. Wrapper-only: party banter. Pass.
- (event) Spec finalized.
