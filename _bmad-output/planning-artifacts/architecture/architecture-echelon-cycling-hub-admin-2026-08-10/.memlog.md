---
scope: Workshop Tasks feature
purpose: Build substrate only
altitude: feature
updated: 2026-08-18T14:43
---

- (direction) Use the Coaching path initially; the user may switch to the Fast path later.
- (decision) The architecture spine is the only deliverable; no additional human-facing artifact is required.
- (constraint) Final PRD source-of-truth partition: Booqable owns order lifecycle, current order-bike membership, physical-bike identifier, rental timing, category/configuration, accessories, and extra_information; Workshop Tasks owns derived workshop lifecycle, assignments, Work Cycles, Item outcomes, Notes, Structured Modifications, Needs Attention, overrides, and audit history.
- (decision) In the 2026-08-10 Booqable foundation brainstorm, the user chose a selective local warehouse rather than a full mirror; each synchronized Booqable entity must be justified by a concrete local workflow dependency.
- (constraint) Brownfield structure is a Next.js/Supabase modular monolith: route Server Components plus route-local clients depend on src/lib feature loaders/actions/types; authenticated mutations use withAuth and discriminated results; PostgreSQL with RLS is the durable authority.
- (constraint) Current Booqable integration uses a thin webhook that identifies an order, refetches current Booqable state, and performs replay-safe but non-transactional sequential upserts under a Vercel Hobby 10-second function limit; there is no queue, worker, pg_net, pg_cron, or Edge Function infrastructure.
- (question) Technical discovery must verify stable Booqable representation for the five Setup Categories, bundled parent linkage, physical-bike identity, and the picked-up/active-rental boundary; failed discovery re-scopes dependent behavior rather than permitting title parsing or guessed associations.
- (decision) Adopt a database-centered transactional modular monolith: Next.js provides the interface and authenticated entry points, while PostgreSQL functions atomically own Workshop Tasks lifecycle changes, claims, reconciliation, snapshots, concurrency checks, and audit recording.
- (direction) Explain architecture decisions in simpler non-specialist language, while still giving a clear recommendation and trade-off.
- (decision) Each Booqable order refresh is committed as one all-or-nothing database transaction across separate normalized entity tables and derived Workshop Tasks changes; the design does not collapse entities into one table or JSON blob.
- (decision) Separate Booqable integration from Workshop Tasks: the integration module alone translates external Booqable responses into the selective local warehouse; Workshop Tasks depends only on stable local tables/contracts and never parses raw Booqable payloads or calls Booqable directly.
- (decision) Store each Bike Task's current lifecycle stage explicitly. Only database transition functions may change it, and they must derive and verify transition eligibility from confirmed checklist outcomes, assignment, current Work Cycle, and synchronized Booqable state. Preserve every accepted stage change in history.
- (decision) Use current-state tables plus an append-only Workshop Task event ledger, not event sourcing. Every accepted domain mutation updates current state and inserts attributable history in the same database transaction; normal application paths cannot edit or delete history rows.
- (decision) Use optimistic concurrency with explicit revisions: lifecycle, ownership, and synchronized-context mutations must provide the expected Bike Task revision and reject stale writes; accepted state-changing mutations increment the revision. Claims additionally use an atomic availability predicate so first valid writer wins. Stale failures return the latest authoritative state while clients retain unsaved typed input when possible.
- (decision) Checklist Templates use editable drafts and immutable activated versions. A Bike Task snapshot references the exact immutable template version and stores task-specific outcomes separately; changing published content creates a new version, and published versions referenced by tasks cannot be edited or deleted. Prep version is fixed at task creation and Return version when return work begins.
- (decision) Represent each preparation/reopening round as an explicit Work Cycle record under the persistent Bike Task; each cycle owns its M1/M2 identities, affected Item work, two-person override scope, and completion, preserving prior cycles without overwriting them.
- (direction) Switch from Coaching to Fast path for remaining decisions; proceed autonomously with assumptions clearly marked and ask the user only when a missing choice materially changes the product or makes implementation unsafe.
- (decision) All multi-row Workshop Tasks mutations are named PostgreSQL RPCs. User-facing server actions remain thin withAuth adapters; expected conflicts return typed failure codes and latest authoritative revision, while unexpected database failures are logged and surfaced through the established error result convention.
- (decision) The Booqable adapter fetches current API state, validates and normalizes it, then submits one complete order snapshot to a service-only ingestion RPC. The RPC locks the order integration record, compares source updated time plus a semantic fingerprint, and atomically updates warehouse rows, derived tasks, invalidations, lifecycle effects, and audit; duplicate snapshots are no-ops and older snapshots cannot regress accepted state.
- (decision) The selective warehouse stores normalized entities and stable external identifiers, not a permanent raw Booqable payload mirror. Store only minimal sync metadata, source timestamps, semantic fingerprints, and failure codes needed for convergence and diagnosis; avoid duplicating unused Booqable data and PII.
- (decision) Bike Task identity is persistent per rental and physical bike: provisional tasks use order plus stable external line/membership identity; when a stable external stock item arrives the same task is promoted. Same-bike removal/reactivation resumes the eligible non-Replaced task; a different physical-bike identity terminally replaces the old task and creates a new task. Never infer identity from titles.
- (decision) Instantiate task checklist Items from the frozen template version and store outcomes separately by task Item, phase, Work Cycle where applicable, and mechanic role. Selective reopening creates new required outcome work only for invalidated Items and never edits prior confirmed outcomes; current effective progress is a database read model.
- (decision) Keep lifecycle, Needs Attention, Structured Modifications, Notes, and modification acknowledgements as separate records: attention cannot block Done; Structured Modifications are durable records with separate Return acknowledgements; Notes remain one mutable latest value and are not the physical-change ledger.
- (decision) Authenticated users receive RLS-protected reads only; direct domain-table writes are revoked. SECURITY DEFINER mutation RPCs use a fixed search_path, derive actor identity from auth.uid(), enforce staff role, assignment, stage, and revision internally, and are explicitly granted only to authenticated. The service-only ingestion RPC is not executable by authenticated users. Partners have no Workshop Tasks access.
- (decision) Queue, task-detail, manager-attention, progress, and last-touch reads are PostgreSQL security-invoker views or read RPCs; no aggregation or cross-table workflow math runs in React/Node. Search, pagination, queue filters, and selected task state use URL parameters. Realtime notifications only trigger authoritative server refresh and never mutate client state as truth.
- (decision) Initial selective-warehouse population is a resumable, bounded operator backfill that processes one order snapshot per transaction through the same ingestion path as webhooks. Do not run an unbounded all-orders loop inside a Vercel request; failed items are retryable without duplicating tasks or history.
- (version) Reality check 2026-08-10: Booqable API v4 is JSON:API and publicly documented as beta; orders, lines, stock items, stock-item plannings, and webhooks exist, but exact Echelon account fields and relationships must be verified against real payloads. Validate adapter inputs with Zod 4.4.3 and fail closed on schema mismatch.
- (constraint) Deploy schema/functions only through idempotent migrations applied locally for verification and remotely by existing staging/main CI. Use additive rollout: schema and disabled integration contract first, bounded backfill/validation second, then Workshop Tasks reconciliation/UI activation; no manual remote DDL.
- (question) Automated test tooling is currently absent. Database state-machine, reconciliation, idempotency, RLS, and concurrency tests are mandatory before implementation handoff, but selecting and introducing the test harness must follow the project rule requiring user choice; official Supabase pgTAP plus a TypeScript adapter runner is the current recommendation.
- (decision) Use a minimum safety test floor during implementation: pgTAP database tests ship with state-machine, RLS, idempotency, and concurrent-claim changes. Broader TypeScript adapter and UI tests may follow after core implementation, but the paper workflow cannot be retired until the critical database suite passes.
- (decision) Synchronized order-bike membership and stock identity rows that are referenced by Workshop Tasks are soft-retired from the current snapshot rather than hard-deleted or cascade-deleted. Reconciliation may remove unreferenced transient rows, but task history must retain stable source references after removal, cancellation, and replacement.
- (decision) Inherit the latest selective-warehouse authority decision: Booqable remains authoritative for orders, customers, and inventory CRUD; local projected source fields are one-way read-only and synchronized Booqable entities are archived/tombstoned rather than hard-deleted so historical references survive.
- (decision) Use layered freshness: entity/order webhooks trigger bounded canonical refresh, nightly paginated reconciliation repairs missed or unsupported events, and consequential Workshop lifecycle transitions perform just-in-time order refresh before the database transition.
- (decision) Persist webhook delivery/processing state so failed events are visible in a minimal sync-operations UI and can be retried safely. Own an environment-specific webhook subscription manifest and repeatable setup/audit script in the repository; secrets remain environment configuration.
- (decision) The selective warehouse models app-owned stable concepts rather than copying the Booqable API schema; external IDs and relationships are preserved at the adapter boundary, while local entity names and workflow semantics remain stable.
- (override) Exclude brainstorm-booqable-data-mirror-2026-08-10 from this architecture run; it is a separate unfinished exploration and not an input. Supersede every prior architecture-memlog entry derived only from it, including selective-warehouse scope, global archival projections, nightly reconciliation, just-in-time refresh, durable webhook operations UI/retry, and webhook subscription manifest decisions.
- (constraint) Architecture inputs are limited to the completed per-bike Workshop Tasks brainstorm, the finalized Workshop Tasks PRD/addendum/memlog, finalized UX package, brownfield code/project context, and decisions made directly in this architecture conversation. Final PRD decisions override earlier brainstorm candidates where they differ.
- (event) Architecture Fast-path session paused for a new agent window after draft, source reconciliation, and reviewer reports. Resume from resume-handoff.md and the memlog; spine remains draft and the next blocker is choosing how atomic ingestion evolves the existing shared Booqable order sync.
- (decision) Evolve the existing shared Booqable orders/order_items persistence into the canonical atomic ingestion RPC while preserving current bookings, orders, and partner-attribution consumers. The RPC becomes the sole writer for Workshop-relevant shared source rows; webhook, sandbox, and backfill paths use it, old direct DML is retired, and no parallel Workshop order projection or second writer is allowed. This binds the brownfield cutover and prevents the existing app and Workshop Tasks from disagreeing.
- (decision) Post-override projection boundary: retain the existing shared customers, orders, and order_items relations as canonical app-wide Booqable facts, evolved additively for current consumers. The Booqable integration module owns any additional canonical membership, physical-bike, sync-state, and versioned normalized-source relations required by Workshop Tasks; Workshop owns only task state and task-specific attestation context. No permanent raw payload mirror is stored. This binds source ownership without reviving the excluded broader warehouse design.
- (decision) Represent Bike Task state as an ordinary workflow stage plus a separate reversible source-availability overlay and an optional irreversible terminal outcome. Cancellation or temporary membership removal makes a task non-actionable while preserving its prior safe stage and history; valid same-bike reactivation clears the overlay and resumes that task under assignment rules. Replaced and force-closed are irreversible; Done remains final. Authoritative returned state forces unfinished non-terminal work into Return and dominates cancellation/removal actionability. This prevents epics from encoding cancellation both as reversible and terminal.
- (decision) Canonical Work Cycle semantics: open one cycle when initial Prep becomes actionable; relevant change during active M1 Prep stays in that cycle and advances only affected requirement generations; relevant change from Re-check or Preparation Resolved closes the prior cycle and opens one new Prep cycle; manager reset closes and replaces the current cycle; forced Return, replacement, force-close, and Done close it; cancellation/removal pauses rather than duplicates it. Preserve each outcome actor. Reassignment changes current ownership without rewriting outcomes, and the mechanic who performs the accepted handoff is that cycle's M1 for M2 independence.
- (decision) Bind checklist semantics across template, execution, and reconciliation: Action outcomes are distinct Done or N/A; required Value Items need a value, optional Value Items may remain blank, and Value Items never offer N/A; M2-enabled implies M1-enabled; M2 records fresh Action resolution or Value verification and never reuses M1 outcome; all admin-authored Items remain visible; Setup Category links control grouping/context/invalidation, not visibility. These discriminators are domain invariants, not deferred ordinary schema detail.
- (decision) Every Item save carries expected task revision plus the exact active requirement generation and source-context generation it attests. The RPC locks and rechecks task stage, assignment, role, and generations; accepted outcomes are immutable for that generation. Reconciliation creates new affected generations and never makes an older outcome current. Item saves advance the task workflow revision only when they change effective confirmed progress; accepted no-ops and rejected commands do not. Lifecycle completion compares the latest revision and all active required generations.
- (decision) Use one versioned normalized order-snapshot contract. PostgreSQL derives the semantic fingerprint from canonical normalized values under that schema version; ordering and null rules are part of the contract. Each refresh receives a database-issued per-order attempt generation before fetch and carries available per-resource source versions. Older attempts or source vectors cannot regress state; same-version/different-content or incomparable versions return a typed conflict, mutate nothing, and require bounded refetch/operator resolution rather than arbitrary hash precedence.
- (decision) Harden capability RPCs and audit storage: each SECURITY DEFINER function is non-login-owned, uses search_path='', schema-qualified objects, rejects null auth actors for user commands, and has ALL revoked from PUBLIC, anon, authenticated, and service_role before the one minimum EXECUTE grant. User RPCs grant authenticated only; ingestion grants service_role only. Direct event INSERT/UPDATE/DELETE/TRUNCATE is revoked from application roles including service_role, events are inserted only inside owned domain functions, parent FKs restrict deletion, and events carry resulting task revision plus transaction-local ordinal for stable causal order.
- (decision) Roll out through database-owned disabled/pilot/enabled activation state and an explicit eligible cohort. Shared order ingestion may run while Workshop derivation/actions remain disabled. Bootstrap creates actionable digital tasks only for eligible rentals first observed reserved and pre-pickup; later-phase pre-cutover rentals remain on paper rather than fabricating Prep history. Backfill uses durable keyset checkpoints and per-order results through the canonical RPC. Rollback disables Workshop derivation/actions/UI without deleting additive schema, shared order data, task history, or audit.
- (decision) Operational proof replaces unsupported platform assumptions: Booqable webhook retries are unverified, so non-2xx is best-effort and every failed/rejected reconciliation emits a greppable contextual log plus an idempotent integration incident keyed by order, snapshot identity, and error code. Paper cannot retire until the pilot cohort has zero unresolved sync incidents and either retry behavior is proven or a durable missed-delivery recovery mechanism exists. Verify the actual Vercel duration setting and measure bounded fetch/RPC budgets before activation; do not bind an unverified 10-second limit.
- (decision) Testing floor is split by mechanism: pgTAP proves database transitions, constraints, RLS/privileges, idempotency, and stale-generation rejection; a true multi-session database/client harness proves concurrent first-writer-wins claims; versioned TypeScript adapter fixtures prove every activated Booqable mapping and canonical snapshot. The exact non-database test runner remains an implementation choice, but those fixture and race tests must ship before their behavior activates.
- (decision) Replace the existing client-only src/app/workshop/page.tsx KanbanBoard mock with the server-loaded Workshop Tasks dashboard and task routes. Preserve compatible route guards, layout, and loading conventions; do not keep the generic Kanban as a parallel workflow.
- (decision) The integration module mints one immutable local order-bike-membership UUID for each proven normalized Booqable bike-line/planning instance; Workshop Tasks keys task identity only to that local membership. Physical-bike promotion attaches a stable stock-item reference without changing the membership UUID. Replacement irreversibly closes the old membership/task and creates a new membership/task; re-add after Replaced also creates new. If the adapter cannot deterministically map external line/planning identities to one membership, it records a rejected reconciliation incident and creates no guessed task.
- (decision) Define one task workflow revision for changes that affect actionability, assignment, lifecycle/terminal state, current Work Cycle, active requirement generations, effective confirmed progress, source context, or completion gates. Notes and orthogonal attention/modification records use their own revisions unless their mutation changes a task completion gate. Accepted no-ops and rejected commands do not increment. All overlapping commands lock in one order: order sync, memberships, tasks by stable ID, cycles/requirements/outcomes/context, orthogonal records, then events.
- (decision) Attention uses a canonical open identity of task plus source plus stable reason code; repeated occurrences refresh/correlate the same open issue, while resolution is separately attributable and does not erase history. Rejected integration incidents may attach to a task only through already-trusted persisted identity. Compound manager commands own their attention resolution and resulting override/assignment in one transaction.
- (decision) Template activation is one transactional capability with exactly one active version per bike category and phase. It atomically supersedes the prior active pointer/version, and task creation or Return entry deterministically pins the active version under the same serialization rule; missing active templates return a typed blocking result rather than an empty checklist.
- (decision) Database constraints enforce minimum aggregate cardinalities: one sync state per order, one active non-Replaced task per membership instance, at most one current Work Cycle per task, one active template per category/phase, one active requirement generation per task Item/phase/cycle/role, one acknowledgement per modification and Return generation, and actor XOR system source on events. These are not deferred indexes.
- (decision) Workshop RPCs use one canonical stable result-code vocabulary defined by the SQL contract and mirrored in src/lib/workshop-tasks/types; expected stale, assignment, stage, generation, authorization, and integration-conflict results map to typed discriminants, while unexpected database failures remain ordinary logged error results.
- (decision) Amend lifecycle precedence to match the finalized PRD: Return forcing applies only to a currently associated, Return-eligible membership. Cancellation or temporary removal remains read-only/non-actionable and cannot enter Return until authoritative reactivation/reassociation clears the overlay; Replaced never reactivates.
- (decision) Amend the revision contract to permit independent Item saves: task workflow revision covers stage/overlay/terminal state, assignment, current cycle, and source/requirement generations, but Item evidence has its own compare-and-set revision and idempotency key. Item saves require current workflow revision/generation guards plus expected evidence revision; disjoint saves may commute when guards remain unchanged. Every accepted evidence edit advances its evidence revision, preserves superseded evidence, and task transitions lock/re-read all active confirmed evidence rather than trusting a progress counter.
- (decision) Workshop owns immutable stable built-in requirement definitions injected into every Prep snapshot: extra_information confirmation is active and required M1/M2 from creation; review_updated_configuration is present but dormant until ambiguous relevant change activates/advances its generation. Reconciliation, outcome saves, completion, and Activity all use these stable identities; admins cannot edit or delete them.
- (decision) Two-person override requests and approvals are scoped to exact Work Cycle, M1 identity, Re-check phase, and expected task revision. Approval is consumed by the authorized same-mechanic Re-check assignment for that cycle; cycle/M1/stage/terminal changes expire and attributably close stale requests. Overrides never carry into a later cycle.
- (decision) In v1 there is no presence lease proving a mechanic remains actively working during source suspension. Cancellation or temporary removal therefore atomically clears current assignment while preserving stage/outcomes; valid reactivation returns the task unassigned at the preserved safe stage. The reconciliation command owns the assignment change and event.
- (decision) Cut over shared Booqable writes with expand-switch-contract across deploys: first add the backward-compatible ingestion RPC without revoking legacy DML; second deploy and verify webhook, sandbox, backfill, and recovery callers use it in each environment; third revoke legacy source-table writes and remove direct DML. Rollback-safe application versions are those compatible with the current phase; never tighten privileges before all deployed callers have switched.
- (decision) Clarify snapshot acceptance: local attempt generation is only a fencing/idempotency token. Freshness is determined solely by the normalized schema version and proven per-resource source-version vector, including shared resources locked by stable external ID. Componentwise newer accepts, identical plus same fingerprint is no-op, and older/incomparable or identical plus different fingerprint quarantines without mutation. A normalizer version changes only through a disabled explicit rebaseline that recomputes canonical fingerprints without workflow invalidation before re-enabling; mappings without proven source versioning cannot activate.
- (decision) Tighten identity cardinality: an immutable normalized membership instance, including proven quantity/planning-instance disambiguation, has at most one Bike Task for its lifetime. Done, Force-closed, cancellation, rollback, or re-enable never creates another task for that membership. Replacement or post-Replaced re-add must mint a new membership before creating a new task.
- (decision) Complete database authority model: non-login object-owner roles own relations; separate non-login BYPASSRLS capability roles receive only needed schema/table/sequence privileges and own non-exposed internal definer functions; exposed RPCs are thin narrowly granted wrappers. Capability roles may insert but never own/update/delete/truncate the event table. Ingestion capability writes only Booqable-owned source fields and allowed reconciliation aggregates; legitimate local customer creation uses a separate capability that cannot write Booqable-owned fields. Direct authoritative source DML is revoked from API roles after cutover.
- (decision) Rollout persists explicit per-order/membership enrollment and last-derived source watermark tied to a rollout epoch. Pilot enrollment is an operator decision confirming the rental has not already been prepared on paper; after general cutover, only newly observed eligible memberships auto-enroll while legacy paper rentals stay excluded. Re-enable remains gated until bounded reconciliation advances each enrolled task from its watermark to current authority, recording bootstrap/resume observations without inventing intermediate history.
- (decision) Minimum operator recovery contract: one authoritative unresolved-incident read query, one authorized bounded refetch/retry command through the same adapter/ingestion RPC, attributable resolution state, and a zero-unresolved check at pilot activation, paper retirement, rollback, and re-enable. Delivery may be a repository script or internal surface; manual source/task DML is prohibited.
- (decision) Every consequence-bearing control exposes in-context pending/error state, disables repeat submission, and shows success only after the server-confirmed result. Item failures retain open-screen typed input for retry; lifecycle commands flush/await pending Item saves, use confirmed evidence only, and remain pending until authoritative rendered state confirms the transition. Offline/session-loss recovery remains excluded.
- (decision) Template activation enforces structural invariants but never requires Setup Category coverage; missing category links may be shown as non-blocking context only.
- (constraint) AD-11 partner exclusion applies only to Workshop-owned relations and read models. Existing partner-scoped access to shared canonical Booqable customers/orders/items remains intact, while those policies expose no Workshop task state.
- (decision) Critical verification and pilot approval are mandatory release procedures, not controls currently enforced by CI. Before the first Workshop migration reaches pilot, pin one locally tested Supabase CLI version in both deployment workflows; CI enforcement of the broader gates may be added, but remote DDL remains CI-only.
- (decision) Selective invalidation always compares latest accepted normalized intent with each requirement's active last-physically-attested source context, never merely the previous ingested snapshot. Accepted mechanic evidence advances that baseline only for the requirement/role/generation it satisfies. Later source refreshes while affected work is unresolved supersede/converge active requirement/source generations to latest intent while preserving prior evidence, and reuse the already-open source-driven Prep cycle unless another explicit lifecycle boundary requires a new cycle.
- (event) spine finalized
- (constraint) The completed Booqable selective-warehouse report is now an authoritative architecture input: it proves the live multi-entity source graph, exact stopped-order bike assignment coverage, measured recovery model, and current operating envelope; its verified findings supersede unresolved pre-spike assumptions.
- (decision) Amend the one shared canonical Booqable projection to the minimum safe multi-entity boundary: preserve existing shared customers, orders, and order_items, and admit business-approved bike ProductGroups/Products, Bundles/BundleItems where Workshop needs them, Plannings, StockItemPlannings, physical StockItems, immutable historical order-bike memberships, and integration event/run state. Booqable owns projected source fields; Workshop owns task state, attestations, and modifications. No parallel Workshop projection, broad mirror, or permanent raw payload store is allowed.
- (decision) Treat Order→Line→Planning→StockItemPlanning→StockItem as the proven physical-bike identity path. Exact persisted relationships create memberships; missing assignment remains explicit unknown and never triggers title inference. Archived source entities and historical assignments are retained/closed rather than destructively overwritten.
- (decision) Freshness uses one canonical adapter/ingestion path with three layers: webhook-triggered targeted refresh for latency, just-in-time canonical order refresh before consequential Workshop transitions, and nightly bounded checkpointed reconciliation for correctness. All admitted resources expose observed updated_at source versions. Standalone StockItem/StockItemPlanning collections are an efficient observed capability only; production retains the documented nested-order-include fallback.
- (decision) Webhook delivery is not a correctness boundary because Booqable publishes no dependable event catalogue or retry guarantee. Persist an idempotent event receipt before external I/O, acknowledge promptly, record attempts/success/failure, and retry bounded work through the canonical path. Reconciliation persists page/keyset checkpoints plus overlap control and repairs missed, unsupported, failed, duplicate, delayed, and out-of-order paths without manual source-table DML. This requirement is no longer Deferred.
- (decision) The Booqable integration foundation precedes dependent Workshop implementation and activation: first stop webhook secret logging and contain the unauthenticated sandbox backfill route; then add backward-compatible projection/inbox/checkpoint/ingestion structures, typed adapters, atomic ingestion, fixtures/tests, bounded shadow reconciliation, failed-event/freshness visibility, and switch all callers; only after the shared contract is proven may Workshop derivation, actions, pilot enrollment, or paper retirement depend on it.
- (version) Spike reality check 2026-08-10: 345 order-detail requests completed in 84.95 seconds with three bounded 429 recoveries; Vercel Hobby Fluid Compute documents 300 seconds while legacy projects may differ, and this deployment's actual mode remains unverified. Supersede the earlier hard-coded 10-second architecture assumption; checkpointing remains mandatory under every duration.
- (question) Before affected Workshop behavior activates, the business must approve the stable ProductGroup-ID bike allowlist and prove the picked-up/active-rental source field; newly observed unmapped trackable groups fail closed. Customer archival/PII anonymization remains a separate bounded policy decision before automated archived-customer handling.
- (question) The spike proves exact Planning→StockItemPlanning→StockItem links but not a stable one-membership-per-unit discriminator for every multi-quantity assigned/unknown/replacement/removal/re-add case. Such mappings must remain fail-closed until source-backed fixture/live proof exists; do not describe quantity disambiguation as already proven.
- (decision) Reconciliation advances each durable checkpoint only with successful application of its page/entity unit, never ahead of uncommitted projection work, and reports assignment coverage/gaps by order status so legitimate draft/cancelled unknowns cannot hide active/completed regressions.
- (constraint) The architecture commitment is final but production activation remains conditional on approved classification, remaining field proof or re-scoping, production fixtures/privilege tests, complete caller cutover, measured operating proof, and recoverable integration health.
- (decision) Use a read-only or least-privileged Booqable integration identity where supported, and expose freshness diagnostics that separate source authentication/rate limiting, adapter-contract failure, and hosting/database unavailability or inactivity pause where observable.
- (decision) Bind one authoritative versioned integration schema package mirrored by fixture-checked TypeScript/PostgreSQL contracts. It defines tagged order_graph and resource_batch units, complete/partial scope, canonical ordering/nulls, explicit known/unknown/removed assignment state, source-version map, and fingerprint inputs. Only complete newer order scopes may close relationships; partial/filtered/failed/not-seen collection data never implies deletion.
- (decision) The ingestion coordinator is the sole source writer and invokes a Workshop-owned internal derivation function in the same transaction. Resource batches may refresh source rows but cannot derive memberships/tasks. Ingestion returns accepted source/derivation watermarks; consequential transitions must provide and verify the watermark under order/task locks.
- (decision) Membership natural identity is order external ID plus line external ID plus source unit discriminator plus incarnation. Single quantity uses discriminator single; planning/SIP IDs are observations, unknown→known preserves incarnation, same-stock planning remap preserves identity, and different-stock replacement/post-Replaced re-add increments incarnation. Multi-quantity remains an activation blocker until the discriminator is proven or FR-1 is re-scoped.
- (decision) Separate append-only webhook transport receipts from coalesced entity refresh intents and append-only attempts. Supabase Cron invokes a protected bounded Next.js worker at least once per minute; database lease generations/CAS prevent stale-worker completion. Admin/manager retry creates attributable new intent/attempt lineage rather than resetting history.
- (decision) Reconciliation uses durable coverage epochs, lease fencing, per-entity progress-safe outcomes, and page checkpoints that advance only atomically with durable results. Quarantine records an incident and permits a completed_with_gaps run to continue. Page scans never infer deletion from not-seen rows; aggregate drift causes overlap/restart and explicit archive or complete order scopes own closure.
- (decision) Preserve shared brownfield semantics through a migration-owned field-authority manifest classifying source/app/derived/compatibility fields. Local and Booqable customer identities do not auto-merge by PII. Partner attribution is app-derived and recomputed atomically from accepted source facts plus app partner mapping. Retained order items expose explicit current/closed state and existing readers use the filtered current contract.
- (decision) Mechanics receive field-minimized task-scoped order/customer context through a narrowly owned read capability, not broad shared-table RLS. Revoke PUBLIC schema/function/default privileges as well as API-role rights; capability roles are NOLOGIN and privilege fixtures execute as anon, authenticated, and service_role.
- (constraint) Integration expansion security/technology gates now include Supabase SSR 0.10 cache-header handling, a supported Next.js LTS before production activation, pinned supported Node runtime, verified Vercel mode/duration, verified remote PostgreSQL/extensions, pinned tested Supabase CLI, and existing-consumer compatibility fixtures. Deployment order is local→staging disabled proof→production disabled proof→production pilot→general activation.
- (decision) Attention contracts use three stable mechanic reason codes: same-mechanic Re-check override with no notes and requester-only block; missing/unclear bike-order information and manager-decision-needed with required creation explanation and manager resolution note. Found-and-fixed is a separate factual Activity record. Task events allocate immutable per-task sequence; template activation/snapshot selection share one category/phase advisory lock.
- (constraint) AD-6 intentionally narrows PRD FR-3 because v1 has no reliable active-presence lease: cancellation/removal always clears assignment and reactivation returns unassigned. Align the PRD before decomposition or separately approve an enforceable presence mechanism; do not silently implement both contracts.
- (decision) Bad-ingestion recovery freezes workers, redeploys the known-good normalizer, refetches affected authority by schema version/watermark, and appends corrective domain events without rolling back app-authored evidence. Domain history persists; open/failed operations never purge; successful operational detail may compact only after one repository-configured window while summaries/freshness/audit references remain.
- (decision) Complete snapshot comparison uses the union of accepted/incoming resource components: unchanged may equal; additions/removals require newer complete-scope root/relationship authority; equal vector plus equal fingerprint is no-op and equal vector plus different fingerprint quarantines with no mutation. Absence closure cannot activate until the authoritative version behavior is fixture-proven.
- (decision) A derivation-relevant resource_batch writes affected-order derivation debt, and order_graph consumes it atomically even when shared source DML is otherwise no-op. Partner-map/allowlist changes advance local derived-context revisions and enqueue affected orders. Disabled Workshop mode keeps the final derivation signature as a stable no-op that records watermarks.
- (decision) Many webhook receipts may correlate through a join to one pending/leased per-entity/schema refresh intent. Intents have explicit claimable/leased/succeeded/exhausted/quarantined/rejected-terminal states, lease expiry/generation/heartbeat/CAS reclaim, bounded append-only attempts, and successor lineages for later signals/operator retry. JIT authorizes transitions only from current applied/no-op outcomes; quarantine/rejection fails closed.
- (decision) User commands that change assignment, stage, terminal outcome, Work Cycle, or source/requirement generation require synchronous JIT freshness; evidence, Notes, attention, modifications, and template commands remain local-only unless they alter a completion gate.
- (decision) Page-number reconciliation uses a documented stable sort, one-page overlap, lease-fenced coverage epochs, and two consecutive complete sweeps with stable seen-ID/source-fingerprint coverage before completion; count equality alone never proves coverage and page absence never deletes.
- (decision) Rollout enrollment uses a completed two-sweep boundary manifest of known order IDs rather than ambiguous timestamps. Blocking incident gates are scoped to the target environment/schema/epoch/cohort, admitted derivation/security resources, high severity, and active/started/stopped orders. Emergency disable/repair never waits on incident count.
- (decision) Persist producer/schema/attempt/epoch provenance for accepted source and derivation. A narrowly owned integration-correction capability may supersede false source-derived current state after a bad normalizer while preserving all app-authored evidence and the correction chain; ordinary lifecycle RPCs never undo terminal outcomes.
- (decision) Create versioned repository-owned task-context and Workshop event contracts. Task context exposes only task-scoped bike/order number/customer display name/address/rental dates/config/accessories/Notes/extra information and excludes contact/demographic PII. Event catalogue binds producers, type/system-source vocabulary, references/revisions, payloads, display fallback, and evolution.
- (decision) Bike ProductGroup allowlists are immutable approved versions. Changes run disabled reclassification epochs and affected-order impact reports: additions require explicit backfill/enrollment; removals stop new enrollment but do not silently close existing tasks, which become operator incidents. Orders retain the applied allowlist version.
- (decision) Apply locked default privileges for every object-creating owner role and prohibit API-role membership/SET ROLE into owner or capability roles. Compact old successful and failed attempt detail under separate configured windows while retaining nonterminal work, first/latest failure, totals, bounded redacted context, provenance, freshness, and audit links.
- (decision) Fence receipt coalescing with monotonic receipt generation: claim/fetch captures covered generation; completion requires unchanged lease and receipt generations; any later receipt atomically leaves/creates successor claimable work or triggers a new bounded fetch. Every receipt must be covered by a fetch begun after it.
- (decision) Disabled derivation advances observed source watermark only, retains materialization debt, and returns derivation_disabled. Enrollment/enable forces accepted order_graph derivation for every enrolled order before Workshop routes/actions activate. Orders absent from the cutover boundary manifest require explicit enrollment unless a separately proven source-created sequence classifies them post-boundary.
- (decision) Bad-normalizer correction never amends a false terminal row. A narrowly owned capability creates one linked correction-successor membership/task as authoritative current, preserves original evidence/history read-only, references evidence only when source context/generation remains valid, and reopens the rest. Same-stock reappearance after Done/Force-closed creates an incident unless a new rental identity or correction successor is proven.
- (decision) Ordinary Item outcome/value saves are always local-only even though they affect completion eligibility; the later transition performs JIT refresh and re-reads confirmed evidence under lock. JIT remains mandatory for assignment, lifecycle, cycle, terminal, and source/requirement-generation mutations.
- (event) spine finalized after selective-warehouse spike reconciliation and reviewer gate
- (event) final reviewer gate passed with no critical or high findings
- (constraint) The 2026-08-12 technical activation-blocker research is an authoritative architecture input. It resolves the five architectural ambiguities through verified contracts or safe re-scopes, while production activation remains blocked on documentary approval, account fixtures, containment fixes, supported tooling, privilege tests, and environment proof.
- (decision) Amend AD-4: generic absence is permanently non-closing in v1 even for a transport-complete relationship. Only a validated explicit archive/tombstone or another independently fixture-proven explicit removed state may close source children or Workshop memberships; absence updates observation/incident state only.
- (decision) Amend AD-5: quantity-one lines may use provisional discriminator single. Multi-quantity lines create memberships/tasks only for exact distinct StockItem assignments, keyed by the physical StockItem external ID; Planning and StockItemPlanning IDs remain observations. Any expected-minus-exact shortfall produces one deduplicated incident and no fabricated ordinal membership.
- (decision) Amend AD-13: bike classification requires an approved versioned ProductGroup UUID allowlist. Targeted Setup Category invalidation activates only with stable approved identifiers and fixture-backed normalization for all active mappings; otherwise relevant changes use broad review_updated_configuration. Per-bike phase derives from exact Planning/StockItem context and stays unknown until target-account lifecycle/removal/re-add fixtures pass.
- (decision) Amend AD-14 technology/security gate: independently migrate Next.js 14 through a tested supported LTS path, prefer 16.x after compatibility proof, pin Node 24.x and one tested stable Supabase CLI, apply Supabase SSR 0.10 refresh cache headers, verify PostgreSQL 17 extension parity, stop secret disclosure, remove or strongly guard the service-role sandbox route, and pass effective-role plus multi-session tests before activation.
- (decision) Use one CRON_SECRET-protected bounded Vercel Cron worker route for scheduled inbox/reconciliation execution. Database lease and receipt generations remain the concurrency authority; the documented 300-second Fluid Compute envelope is headroom, not a recovery mechanism.
- (constraint) FR-1 and FR-3 architecture rules are settled, but the owning PRD must be amended before dependent task decomposition. Business approval of actual ProductGroup UUIDs and either stable Setup mappings or the broad-review re-scope remains mandatory.
- (decision) Clarify AD-4 removal authority: an independently versioned explicit child archive/tombstone may authorize closure without parent updated_at advancing; otherwise a fixture-proven relationship authority plus explicit removed state is required.
- (constraint) Carry the research minimum proof matrix as a binding AD-14 floor: complete identity/mapping adapter fixtures, pgTAP comparator/atomicity/debt/cardinality/lifecycle/revision/event/privilege tests, all four named multi-session races, and pinned-CLI reset/test/lint/type generation.
- (decision) Pilot approval also requires a reviewed rollback/repair procedure naming emergency disable, canonical refetch/reconciliation, incident handling, correction-successor use, evidence retention, and resume authority; general activation and paper retirement remain separate approvals.
- (constraint) PRD documentary closure is already committed as of 2026-08-12: FR-1 uses exact-StockItem-only multi-quantity tasks with incident shortfall, FR-3 unconditionally clears assignment, and Setup uses stable mappings or broad review. Replace stale future-amendment gates with a dated closure record.
- (decision) Consequential JIT commands create a monotonic demand generation; only a fetch begun after that demand may authorize the command. Ingestion returns a versioned freshness proof bound to order, schema, attempt, source/derivation watermarks, rollout epoch, and enrollment. The mutation locks and compares proof plus the user-displayed workflow revision; any JIT workflow-revision advance returns stale for reconfirmation rather than silently rebasing.
- (decision) One database-owned environment/rollout-epoch activation state is the sole control plane. Durable attributable admin approvals transition disabled, shadow, pilot, enabled, and emergency_disabled atomically; deployment configuration cannot independently activate. The same database predicate gates derivation, enrollment, reads, context capabilities, and mutations; UI only reflects it.
- (decision) The integration coordinator solely owns and writes immutable order-bike memberships as normalized integration entities before invoking Workshop-owned task derivation in the same transaction. Explicit order cancellation maps to cancelled overlay; explicit Planning/SIP removal maps to temporarily_removed and pauses the cycle; same-stock re-add reactivates the membership, different-stock replacement closes it and creates a linked new incarnation.
- (decision) Fail closed on exact StockItem overassignment or one StockItem assigned to multiple admitted memberships in the same rental graph: quarantine the conflicting graph, create one blocking identity incident, and derive no conflicting memberships/tasks until authoritative refetch resolves it.
- (decision) Comparator omission carries forward accepted component state/version and opens or refreshes an incident; it cannot close or regress the component and does not block independent newer components from applying atomically. Explicit child tombstones remain independently comparable.
- (decision) Correction successors are admin-only through a thin authenticated RPC backed by a separate internal correction capability; anon, mechanics, managers, service_role, and the integration coordinator cannot execute it. The command locks the false predecessor, enforces one immutable successor edge per correction epoch and idempotency key, and concurrent losers return the existing successor.
- (decision) Assignment postconditions are universal: manager reset, source invalidation back to Needs Prep, forced Return, replacement, force-close, Done, cancellation, and temporary removal all clear current assignment atomically; ordinary handoff semantics remain cycle-scoped.
- (decision) Broad Setup fallback fingerprints every admitted normalized configuration, accessory, extra-information, and unknown/null/removed state used by task context; first baseline does not invalidate, every later semantic change advances review_updated_configuration, and broad-targeted mode changes require disabled rebaseline. Exact per-bike stopped authority makes that bike Return-eligible without waiting for full-order stop; conflicting order/bike facts fail closed with incident.
- (decision) Lease generation fences the entire source-plus-derivation transaction; expired attempts return rejected_retryable with no domain mutation. A versioned incident/result-transition catalogue fixes severity, activation impact, retryability, resolution, and result-to-intent state/budget behavior; producers do not choose free-form severity.
- (decision) General enablement persists the completed two-sweep known-order manifest and records one disposition for every eligible pre-boundary order: explicitly enrolled or legacy-paper-excluded. Unaccounted eligible orders block enablement. Only a fixture-proven source-created sequence may auto-enroll post-boundary rentals; ambiguous cases require attributable operator enrollment.
- (decision) Remove the unproven same-stock new-rental exception after Done/Force-closed. Until a versioned source-backed new-rental identity profile is approved with positive/negative/replay fixtures, same order-line-stock reappearance remains incident-only; a new order/line identity or correction successor is required.
- (decision) Physical StockItem exclusivity is cross-order for authoritative half-open rental intervals [starts_at, ends_at). Sequential non-overlapping rentals are allowed; overlapping or unknown intervals for the same StockItem quarantine affected graphs under a StockItem-keyed lock. Removed/terminal task state does not waive interval conflict until source rental authority ends it.
- (decision) New source-component admission requires the versioned producer profile's fixture-proven newer relationship authority or approved independently versioned child-creation token; otherwise it quarantines with no derivation. Fingerprints compare canonical merged effective state after omission carry-forward, while omission observations/incidents remain separate.
- (decision) Correction cardinality is one immutable successor edge per false predecessor independent of correction epoch or idempotency key. Request idempotency deduplicates replay only; later correction targets the current successor and cannot branch from the original predecessor.
- (event) spine finalized after 2026-08-12 activation-blocker research reconciliation; lint passed and final rubric, incompatible-unit, data-integrity, and cross-epic gates reported no critical/high findings
- (constraint) Approved 2026-08-18 MVP PRD replaces the retired FR-1..FR-48 autonomous-reconciliation contract: the feature governs FR-1..FR-21 and NFR-1..NFR-7 only; retired requirements remain non-goals.
- (decision) Preserve the transactional modular monolith, frozen canonical Booqable projection/adapter, RLS, authenticated actions, atomic database mutations, and attributable history; remove MVP commitments to membership-incarnation algebra, freshness proofs, activation/enrollment, correction successors, selective invalidation, and specialized Return engines.
- (decision) One live fetch-and-apply boundary handles webhook signals and task claims: it uses the canonical adapter and apply_canonical_order_graph, preserves sync.ts and all frozen brownfield callers, and treats a failed claim refresh as a typed non-claim result; this is not a signed freshness-proof or retry/reconciliation system.
- (decision) Bike Tasks begin only from a manager-assigned exact StockItem and have a small Task Outcome/Work Phase model. Templates, active-Prep reconfirmation, manager attention/intervention, reusable Return Check, and task history are the MVP workflow; no Work Cycle, same-mechanic override, provisional quantity identity, automatic reactivation, or rollout control plane is introduced.
- (decision) MVP source-driven Workshop changes have one service-only database path: apply_canonical_order_graph calls one internal Workshop derivation function in the same transaction for accepted applied source changes; staff commands remain distinct withAuth/capability RPCs. Return entry atomically releases current ownership, creates the current-category Return Snapshot, advances task revision, preserves interrupted history, and exposes unassigned Needs Return Check.
- (decision) The Bike Task creation key is one Booqable rental/order plus one exact opaque StockItem ID; stock_identifier is display-only. Relevant active-Prep source changes increment a task source-context generation and task revision; acknowledgement is generation-specific, attributed, clears only that generation, and never invalidates evidence. Prep M1/M2 evidence is role-scoped; Return has no M2 or individual acknowledgement.
- (constraint) The approved MVP spine conflicts with retained future-cutover rules in project-context.md and older architecture memlog entries. Preserve only the frozen canonical projection/adapter, brownfield-reader, security, and deployment constraints; project context requires a separately authorized alignment update before Workshop implementation.
- (decision) User chose the simple assignment-removal rule: when an exact StockItem is removed from an active rental without cancellation or replacement, mark the Bike Task Cancelled, clear its mechanic assignment, and preserve history. Reassignment of a different exact StockItem creates the ordinary fresh replacement task.
- (decision) User chose a non-claimable Actionable Work Phase named Awaiting Return after Prep resolves and before Booqable marks the rental returned. It clears owner, records the resolution, is hidden from Available Now/My Work, and transitions to unassigned Needs Return Check only on authoritative return. This new product vocabulary requires a companion PRD wording update.
- (event) spine finalized after 2026-08-18 MVP PRD reconciliation and reviewer gate
- (event) implementation handover prepared
