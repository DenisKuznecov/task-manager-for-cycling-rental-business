---
scope: Automating mechanics' daily physical-bike preparation, re-check, pickup, return, and storage workflow
purpose: build-substrate
altitude: feature
updated: 2026-08-20T17:52
---

- (direction) Fast path selected; produce only a feature-level build substrate and tag inferred architecture choices as [ASSUMPTION] for review.
- (constraint) SPEC-automating-mechanics-daily-work CAP-1 through CAP-10 and all four companions are binding; the brainstorming intent and technical research supply rationale only.
- (constraint) Brownfield reality: Next.js App Router, Supabase PostgreSQL/Auth/RLS/Realtime, Subframe UI, withAuth server actions, URL search-parameter state, security-invoker views, and local-only migration application are inherited conventions.
- (decision) AD-1 [ASSUMPTION] adopts a hexagonal modular monolith with a database-enforced transactional domain core; it binds all capabilities and prevents workflow rules from diverging across UI, route handlers, Booqable adapters, and persistence code.
- (decision) AD-2 [ASSUMPTION] stages one validated complete Booqable snapshot, then applies order projection, current add-ons, source assignments, and task reconciliation in one database transaction; it prevents partial source fetches or sequential writes from exposing mixed source revisions.
- (decision) AD-3 [ADOPTED] uses Booqable order ID plus stock_items.id plus task kind as source identity, with editable bike identifiers as display metadata and source assignment state separate from workflow state; it prevents identity drift and history transfer.
- (decision) AD-4 [ASSUMPTION] stores immutable versioned checklist definitions in database seed migrations and copies definition fields into task checklist item instances at task creation; it prevents later catalog edits from rewriting in-flight or historical work.
- (decision) AD-5 [ASSUMPTION] routes every checklist mutation and lifecycle transition through authenticated server actions and atomic PostgreSQL command functions with expected task version checks; it prevents guard bypass, stale writes, and split signature/status commits.
- (decision) AD-6 [ASSUMPTION] exposes read-only RLS-protected views/tables to staff, revokes authenticated direct DML, and keeps privileged helper functions in an unexposed private schema behind narrowly granted public RPCs; it prevents partner access and browser-side bypass of workflow rules.
- (decision) AD-7 [ASSUMPTION] appends immutable transition and attestation records while keeping current task status as a projection; attestation rows snapshot user ID, first name, last name, time, same-mechanic exception, and readiness add-on context; it prevents mutable profiles or order lines from rewriting accountability.
- (decision) AD-8 [ASSUMPTION] binds readiness confirmation to the exact synchronized add-on revision inside the atomic M2 command; it prevents confirmation of stale add-ons during concurrent synchronization.
- (decision) AD-9 [ADOPTED] computes Today, Tomorrow, and Next 7 days in PostgreSQL using Europe/Madrid and exposes them through a security-invoker read model; URL parameters own queue, search, and task-drawer state, preventing client-side aggregation and divergent timezone logic.
- (decision) AD-10 [ASSUMPTION] uses a persisted lease and resumable paginated batches for manual sync, plus a per-order transaction lock for reconciliation; it prevents overlapping runs, serverless timeout ambiguity, and concurrent set-diff races without adding polling or queue infrastructure.
- (decision) AD-11 [ASSUMPTION] forbids drag-and-drop or arbitrary status updates; state changes occur only through named guarded workflow actions, while Realtime only invalidates server-rendered read models; it prevents the existing mock Kanban interaction from bypassing signatures and guards.
- (decision) AD-12 [ADOPTED] adds no new runtime platform for MVP: Next.js runs on the established Vercel deployment and PostgreSQL 17 remains the durable system of record; migrations are verified locally and reach staging/production only through branch CI.
- (decision) AD-13 [ASSUMPTION] requires local database contract tests for transition guards, RLS, idempotency, cancellation, and concurrency, plus fixture tests for Booqable parsing; it prevents implementation completion based only on UI-path checks.
- (version) Brownfield lockfile reality: Next.js 14.2.35, React 18.2.0, TypeScript 5.9.3, @supabase/supabase-js 2.102.1, @supabase/ssr 0.10.0, @subframe/core 1.154.0, Zod 4.4.3; local Supabase targets PostgreSQL 17.
- (question) Controlled tenant spike must confirm the exact Booqable v4 include path, workshop-tag field, add-on classification, read-after-write lag, webhook behavior, authentication, and retry semantics before the Booqable adapter contract can be finalized.
- (question) The four non-road preparation checklist catalogs remain required before those tags can create startable tasks; architecture may land with those mappings disabled rather than invent content.
- (question) Tenant-spike volume and duration must set manual-sync page and batch limits, lease duration, retry envelope, and any webhook debounce interval.
- (decision) AD-11 amended and adopted by user: delete the mock Kanban and replace it with simple task tables for Today, Tomorrow, and Next 7 days; tables are read models, and status changes remain explicit guarded actions rather than drag-and-drop or arbitrary updates.
- (direction) Use easy, plain language in architecture discussions and handoff.
- (decision) AD-2 amended: complete source input carries an application-computed normalized fingerprint; cross-page consistency detection remains a tenant-spike blocker rather than assuming a Booqable revision token.
- (decision) AD-4 amended: seed the exact road and shared-storage catalogs; create tagless tasks without preparation items, block start, and attach the preparation snapshot after correction while the same task remains To Prepare.
- (decision) AD-5 and AD-7 amended: database commands enforce the exact lifecycle edges, checklist outcome and M2 confirmation semantics, terminal states, no assignment or locks, immutable stage attestations, and immediate abandon-work behavior after invalidation.
- (decision) AD-8 amended: readiness confirms the current add-on fingerprint; add-ons remain visible, later sync updates display, and changes after Ready for Pickup never reopen work.
- (decision) AD-10 amended: manual sync scans all reserved orders starting in the next seven days, skips completed/cancelled orders, persists progress and last success, uses run and per-order leases, and retries network/5xx/429 with exponential backoff, jitter, and Retry-After.
- (decision) AD-13 amended: controlled tenant spike is an implementation gate covering all research scenarios and evidence outputs, with compatibility recheck due 2026-09-20.
- (constraint) Brownfield cut-over replaces the sequential Booqable writer and unauthenticated sandbox sync route; webhook eligibility must come from fetched state and provided secrets must not be logged.
- (decision) AD-12 amended and adopted by user: upgrade to a currently supported Next.js major before implementing this feature; do not accept Next.js 14 as the feature runtime.
- (decision) AD-11 amended and adopted by user: use one task table with Today, Tomorrow, Next 7 days, and All filters; the active filter is URL state.
- (assumption) Next 7 days is a cumulative selected filter from Madrid business date today through six following dates; because only one filter is active, tasks are not duplicated on screen. All means all non-cancelled active tasks.
- (event) User confirmed: use easy language going forward; the current workshop Kanban is a disposable mock and must be deleted; simple task tables replace it.
- (decision) AD-11 amended and adopted by user: opening a table row navigates to a dedicated task page for the checklist and named actions, not a drawer or modal. Route follows the existing detail pattern: /workshop/[taskId].
- (decision) AD-4 adopted by user: checklist definitions are copied onto the task; later catalog edits do not rewrite in-progress or completed work.
- (decision) AD-5 adopted by user: PostgreSQL commands own status changes and guarded transitions; the UI only calls named actions.
- (decision) AD-10 adopted by user: manual sync is paged and leased so it does not time out and two people cannot sync the same order at once. Exact page, batch, lease, and retry numbers stay deferred until the tenant spike.
- (event) User adopted the Next.js upgrade-first gate already recorded in AD-12.
- (decision) Finalize: adopt remaining technical assumptions AD-1, AD-2, AD-6, AD-7, AD-8, AD-13 as binding; they match the spec and brownfield conventions. Next 7 days is the Madrid half-open range [today, today + 7 days) including Today and Tomorrow. Open product items stay deferred: four checklists, tenant spike, sync numbers, staging Booqable policy.
- (version) Web-verified 2026-08-20: Next.js latest stable is 16.3.1 (released 2026-08-13); Vitest latest stable is 4.1.11 (v5 is rc only). Brownfield pins remain Next 14.2.35, React 18.2.0, TypeScript 5.9.3, supabase-js 2.102.1, @supabase/ssr 0.10.0, Subframe 1.154.0, Zod 4.4.3.
- (decision) AD-3/AD-4: a missing, unknown, or ambiguous workshop tag still creates a visible task. Start preparation stays blocked. Do not hide, skip, or cancel the task. The four missing checklists stay disabled.
- (decision) AD-13 success gate: a mixed order with identified, unidentified, removed, and replaced bikes must converge to one valid task per current bike, then a tablet can run the guarded lifecycle through Completed. Unidentified lines must not block identified tasks.
- (decision) AD-11: the workshop UI must show last successful full-horizon sync time and clear sync failures. Storing health in AD-10 is not enough.
- (decision) AD-10: the webhook awaits one bounded reconcile and then returns. It must not detach work. The seed comment must not say the webhook starts a runner.
- (decision) Explicit rules: a date-only start-date change updates queue timing and does not reset checklist work. Skip completed and cancelled source orders. Uniqueness is assignment instance plus task kind, with at most one active instance per order and stock item. M2 confirms M1 PSI or N/A and does not replace those values. Add-on confirmation is required before readiness; fingerprint match is the mechanism.
- (decision) AD-12 stack: Next 16.3.1 with React and React DOM 19.2.8. Do not keep React 18.2.0 as the feature runtime. Next 16 removed next lint; pin eslint-config-next 16.3.1 and ESLint 9, and replace the repo next lint plus eslint-config-next 13.5.4 in the upgrade. Cut over middleware to proxy and make searchParams async, including login.
- (decision) AD-12 ops: pin Supabase CLI 2.115.0 in CI and stop floating latest for this feature. Local Postgres 17 is the known target; do not claim hosted major. Preview and PR deploys must not write staging or production databases or webhooks; preview sync stays off unless an isolated tenant is documented.
- (decision) AD-10: retry 429, 5xx, and network with backoff. Honor Retry-After when the response has it; otherwise use backoff. Official Booqable v4 docs do not make that header a contract.
- (decision) AD-5 and AD-6: staff session is withAuth. Role is get_user_role from auth.uid. Do not add a second wrapper.
- (decision) AD-2 and AD-3: PostgreSQL apply mints assignment-instance IDs. Adapter sends raw stock items and raw add-ons. PostgreSQL alone normalizes and fingerprints. Apply updates existing public.orders and order_items; no parallel source-order schema. One rental_turnaround task per instance; storage is a stage, not a second task kind.
- (decision) AD-8: after ready for pickup, display can update and status never reopens. Removal, replacement, or order cancel still cancels the task.
- (event) spine finalized
