---
idea: Per-physical-bike digital mechanic tasks (M1/M2/storage) created from Booqable snapshot reconciliation of identified stock items
goal: Pressure-test the MVP from brainstorm-intent.md against the Booqable assignment research until it is hardened, killed, or clearer
status: complete
updated: 2026-08-20T16:49
---

- (assumption) Existing webhook already fetches the full order after a signal; current include is customer,coupon,lines and does not yet load stock_item_plannings. Research still treats assignment-event coverage as unverified.
- (decision) The person who assigns the physical bike ID in Booqable is also responsible for opening this app and running Sync. That is an internal operating agreement, treated as cheap.
- (assumption) Webhook delivery for bike-ID assignment is not required for the MVP to function; manual sync by the assigner is the reliable path.
- (decision) Missing-task when the assigner skips Sync is an accepted failure mode for MVP; no proactive unsynced-assignment signal.
- (crack) Claim that no signal is implementable may conflate 'no assignment webhook' with 'cannot fetch upcoming reserved orders'. The same snapshot reconciler used by manual Sync could detect drift if it ran on a timer or dashboard open; that was deferred, not proven impossible.
- (assumption) User belief: Booqable does not emit order.updated when stock_items.id assignment changes. API can still see the new ID on fetch; the gap is the push, not the snapshot.
- (direction) Also run reconciliation when a mechanic opens a task, not only via the assigner's Sync button.
- (decision) First-task creation: assigner runs Sync. Later drift on an order that already has a task: reconcile that order when a mechanic opens the task.
- (note) User asked whether Today/Tomorrow queue actions should also trigger reconciliation.
- (lock) MVP Booqable triggers: (1) assigner Sync creates first tasks and recovers silent orders; (2) opening a task reconciles that order; (3) webhook uses the same reconciler when it fires. No automatic fetch on Today/Tomorrow/Next 7 days. Queue shows last successful sync time.
- (note) Session stopped by user for time. Outcome: clearer on Booqable triggers only; not hardened.
