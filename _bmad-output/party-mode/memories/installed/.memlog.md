---
topic: Booqable order sync -> internal Task Management (Workshop kanban)
goal: Design how Booqable orders become tasks on the workshop board
updated: 2026-08-15T12:02
---

- (note) Opening ask: sync Booqable orders into the Workshop/Task Management kanban. Found: kanban (kanban-types.ts, /workshop) is 100% mocked (SAMPLE_TASKS), no tasks table yet; existing booqable/sync.ts is a thin-webhook->refetch-JSON:API pattern for orders/customers/order_items; OrderStatus enum (draft/new/canceled/reserved/started/stopped/archived) maps closely to kanban columns (new/ready-for-rent/out-for-a-ride/returned/done) but SAMPLE_TASKS descriptions are per-bike/line-item, not per order.
- (dynamic by john) Winston pitched extending the existing sync; Amelia grounded it in 'no tasks table exists yet'; Mary got excited finding the status/column mapping; John pushed hard on order-vs-item task granularity; Sally raised the one-way-sync-stomps-manual-kanban-moves risk. Landed on 3 open decisions for Den: task granularity, auto-create trigger point, sync direction (one-way vs write-back on cancel/archive).
- (outcome) On Workshop Tasks UX, Den separated active manager work Waiting for Bike ID from generic Needs Attention; the first concrete attention request is a mechanic asking Dima for an explicit same-mechanic Re-check override.
- (outcome) Den rejected the happy-path-only pilot after seeing it would strand solo Re-check and exception coordination; manager approvals and lightweight attention requests stay in first release.
- (outcome) The room shaped a lightweight manager-attention contract: Waiting for Bike ID stays separate; override, unclear-info, and manager-decision requests remain; no chat thread, but categorized requests, required context where needed, manager action, resolution notes, and audit all stay.
- (outcome by bmad-agent-architect) Den chose service-role-only access for the canonical Booqable projection; safe user-facing reads must come later through explicit views or RPCs.
- (dynamic) Den walked in asking what to do after Story 2.5's first run blew up and bmad-build-auto HALTed on a leftover spec-2-5-review-diff.txt in the render snapshot. Room split: Amelia/John want delete-the-cache-and-rerun; Mary wants a dirty-tree check first because the leftover diff still smells like the old allowlist story.
- (outcome by bmad-agent-architect) Den chose Winston's cleaner move: wipe the whole b1a16ae7807c5e9c277e snapshot folder, then re-run bmad-build-auto on the updated Story 2.5 spec. No hand-implementation.
- (dynamic) Den walked in angry that canonical-projection.ts is an unreadable contract vs his fetch-save-build-tasks mental model. Winston defended it as the lock the existing sync.ts path never had (orders/lines can't name a bike). Amelia: file is admission-only, no writer, tables empty, no tasks table. John split the anger: graph vs 163-row manifest. Sally: mechanic never sees this; stomped-manual-moves still unanswered. Mary framed five rules / five pipeline steps. Room waiting on whether Den thought order_items already was the bike.
- (moment) Den clarified: he never thought order_items were bikes. He wanted fetch-and-save so we don't guess which bike is which. He doesn't know 'graph'. His own words: the file is a contract between Booqable and our DB — their product data looks like X, transforms like Y, saves like Z. Room will correct: it's our allowed shape + identity rules, not a Booqable API dump; transform/writer still unbuilt.
- (moment by bmad-agent-ux-designer) Den corrected Sally: we are not keeping the mock kanban. Planned UX replaces /workshop KanbanBoard with mechanic queues (My Work / Available Now) and Bike Task detail. Sally's old 'sync stomps dragged cards' grenade is about the board we're deleting. Architecture-spine already says the generic KanbanBoard is replaced.
