---
topic: Booqable physical bike ID assignment detection
type: technical
decision: Choose the smallest reliable integration mechanism for detecting physical bike assignment, removal, and replacement on reserved Booqable orders.
preset: standard
updated: 2026-08-20T16:22
---

- (decision by user) Approved a straightforward technical spike covering webhook coverage, API identity fields, polling and reconciliation options, operational constraints, and a minimum viable detection design; standard effort pruned to eight sources and two rounds with normal validation.
- (assumption by user) Project observation to test: assigning a physical bike ID alone does not trigger the current order-update webhook, but a later webhook-triggering order change includes that ID in its payload.
- (source) Round 1 source batch: current Booqable API v4 and v1 docs, official order-product and individually-tracked-stock help articles, official Zapier help, and API Evangelist's secondary OpenAPI extraction; public evidence exhausted before webhook assignment semantics were established.
- (claim) ref=[1] status=verified class=version/compatibility pub=2026-08 — Current Booqable API v4 documentation exposes physical assignments through lines, planning, stock_item_plannings, and stock_item relationships; the full dotted include request remains to be tenant-tested.
- (claim) ref=[2] status=unverified class=version/compatibility pub=2026-08 — An initial documentation pass observed order.updated, but fresh verification could not confirm it on the current official page and found no documented assignment-trigger semantics.
- (claim) ref=[5] status=verified class=version/compatibility pub=2026-08 — Booqable's human-readable product identifier is adjustable, making stock_items.id the safer documented API identity key without asserting formal immutability.
- (claim) ref=[8] status=verified class=version/compatibility pub=2026-08 — Current public API v4 docs expose authentication, pagination, and HTTP 429 behavior but do not specify numeric quotas or webhook delivery, ordering, retry, duplicate, payload, or read-after-write guarantees.
- (decision) Recommend snapshot reconciliation keyed by order and stock_items.id, triggered by any proven broad order event and backed by a scheduled sweep; replacement is set-diff removal plus addition with no task transfer.
- (event) Final semantic citation audit found references [2] and [8] only partially support broad documentation-silence wording; report was scoped to the retrieved v4 page/retrieved official sources and confidence reduced to medium.
- (claim) ref=[8] status=unverified class=version/compatibility pub=2026-08 — Retrieved v4 docs verify authentication, pagination, signing, and HTTP 429, but the wider absence of webhook delivery details remains only partially supported and is scoped to the retrieved page.
- (event) Applied required structure and prose polish: recommendation and controlled spike moved ahead of evidence, findings merged into two decision arguments, detached open questions converted to spike outputs, redundant synthesis removed, and terminology tightened.
- (event) Research complete: cited Markdown report and self-contained HTML briefing finalized; citation references pass mechanically, semantic audit corrections applied, claim tally is two verified and two unverified, and earliest evidence re-check is 2026-09-20.
- (decision by user) MVP integration decision refined: retain the existing order.updated handler as a signal that triggers a full order API fetch without payload parsing; add a non-overlapping safety sweep every 2–3 minutes over relevant upcoming reserved orders, excluding completed and cancelled orders, and lengthen the interval if measured request volume or HTTP 429 responses require it.
- (decision by user) Supersedes the prior polling decision: for MVP, retain order.updated plus full order re-fetch and add a staff-triggered Sync latest data from Booqable action; do not run automatic periodic polling. Task creation may wait for either trigger. Manual sync must show progress, last successful sync time, success or failure, and use the same idempotent reconciler.
